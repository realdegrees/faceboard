// App version + repository URL, injected at build time by Vite `define`
// (see vite.config.ts). Surfaced in the sidebar footer and Settings → About.
export const APP_VERSION: string = __APP_VERSION__;
export const APP_REPO: string = __APP_REPO__;
